[package]
name = "ferrumc-ecs"
version = "0.1.0"
edition = "2024"
description = "The single source of truth for the ECS schema."

[dependencies]
bevy_ecs = { workspace = true }

ferrumc-core = { workspace = true }
ferrumc-protocol = { workspace = true }
